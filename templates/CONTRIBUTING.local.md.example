<!--
Copy this into the consuming repo's root as CONTRIBUTING.local.md and fill
it in. This content gets appended below the canonical CONTRIBUTING.md
content when sync-guidelines.sh runs — don't repeat rules already covered
there, just what's specific to this repo.
-->

## About this repo

[What this repo is, and its place in the architecture — e.g. "Shared
FastAPI chat service, deploys to per-tenant Container Apps via the CI/CD
matrix" or "Per-tenant dbt models for tenant {name}".]

## Local setup

[Steps to get this repo running locally — env vars needed, how to get a
local `.env` populated, how to run it, how to run tests.]

## Repo-specific notes

[Anything else specific to this repo that doesn't belong in the shared
guidelines — e.g. a quirky dependency, a manual step CI doesn't cover.]
